<Window x:Class="DistrictPolyclinic.Pages.ChangePasswordForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DistrictPolyclinic.Pages"
        xmlns:services="clr-namespace:DistrictPolyclinic.Services"
        mc:Ignorable="d"
        Title="Змінити пароль" Height="290" Width="290"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>
        <Style x:Key="StandardPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryTextColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="StandardTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="FlatTextButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="7" Background="White" Padding="0">
        <DockPanel LastChildFill="True">
            <!-- Top panel (header) -->
            <Border Background="#F5F5F5" DockPanel.Dock="Top" Height="40" CornerRadius="7,7,0,0">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="Змінити пароль" FontSize="17" FontWeight="Bold" Margin="10,8,0,0"/>
                    <Button Content="X" Width="25" Height="25" Margin="110,5,0,0" 
                        DockPanel.Dock="Right" VerticalAlignment="Top" 
                        Click="CloseButton_Click" Style="{StaticResource CloseButton}"/>
                </DockPanel>
            </Border>

            <!-- Main part -->
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,-5,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Старий пароль" Style="{StaticResource StandardTextBlockStyle}" Margin="0 0 0 5"/>
                        <PasswordBox x:Name="txtOldPassword" Style="{StaticResource StandardPasswordBoxStyle}" services:PasswordBoxPlaceholderService.Placeholder="Введіть старий пароль" Height="25" Margin="0 0 0 15"/>

                        <TextBlock Text="Новий пароль" Style="{StaticResource StandardTextBlockStyle}" Margin="0 0 0 5"/>
                        <PasswordBox x:Name="txtNewPassword" Style="{StaticResource StandardPasswordBoxStyle}" services:PasswordBoxPlaceholderService.Placeholder="Введіть новий пароль" Height="25" Margin="0 0 0 15"/>

                        <TextBlock Text="Повторіть новий пароль" Style="{StaticResource StandardTextBlockStyle}" Margin="0 0 0 5"/>
                        <PasswordBox x:Name="txtReplay" Style="{StaticResource StandardPasswordBoxStyle}" services:PasswordBoxPlaceholderService.Placeholder="Введіть новий пароль знову" Height="25" Margin="0 0 0 15"/>
                    </StackPanel>

                </Grid>

                <Separator Margin="0 180 0 0" Background="Gray" Height="1" />

                <!-- Button row -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Скасувати" FontSize="15" 
                        HorizontalAlignment="Left" Margin="0,0,0,5" 
                        Width="90" Style="{StaticResource FlatTextButton}" 
                        Click="CancelButton_Click"/>

                    <Button Grid.Column="1" Content="Змінити" FontSize="15" 
                        HorizontalAlignment="Right" Margin="0,0,-8,5" 
                        Foreground="Blue" Width="90" Style="{StaticResource FlatTextButton}"
                        Click="ChangePassword_Click"/>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>

