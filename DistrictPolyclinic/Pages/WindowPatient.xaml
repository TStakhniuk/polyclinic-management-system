<Window x:Class="DistrictPolyclinic.Pages.WindowPatient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DistrictPolyclinic.Pages"
        xmlns:services="clr-namespace:DistrictPolyclinic.Services"
        mc:Ignorable="d"
        Title="WindowPatient"  Height="170" Width="290"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">

    <Window.Resources>

        <!-- Style for TextBlock (headings) -->
        <Style x:Key="StandardTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Style for ComboBox -->
<Style x:Key="StandardComboBoxStyle" TargetType="ComboBox">
    <Setter Property="Height" Value="25"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
</Style>

        <Style x:Key="FlatTextButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for buttons without background, with red background on hover -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="7" Background="White" Padding="0">
        <DockPanel LastChildFill="True">
            <!-- Top panel (header) -->
            <Border Background="#F5F5F5" DockPanel.Dock="Top" Height="40" CornerRadius="7,7,0,0">
                <DockPanel LastChildFill="True">
                    <TextBlock Text="Медична картка" FontSize="16" FontWeight="Bold" Margin="10,8,0,0"/>
                    <Button Content="X" Width="25" Height="25" Margin="110,5,0,0" 
                        DockPanel.Dock="Right" VerticalAlignment="Top" 
                        Click="CloseButton_Click" Style="{StaticResource CloseButton}"/>
                </DockPanel>
            </Border>

            <!-- Main content -->
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Пацієнт" Style="{StaticResource StandardTextBlockStyle}" Margin="0 0 0 5"/>
                        <ComboBox x:Name="cmbPatient" Style="{StaticResource StandardComboBoxStyle}" services:ComboBoxPlaceholderService.Placeholder="Оберіть пацієнта" IsEditable="True" IsTextSearchEnabled="True" Margin="0 0 0 15"/>
                    </StackPanel>

                </Grid>

                <Separator Margin="0 60 0 0" Background="Gray" Height="1" />

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Скасувати" FontSize="15" 
                        HorizontalAlignment="Left" Margin="0,0,0,5" 
                        Width="90" Style="{StaticResource FlatTextButton}" 
                        Click="CancelButton_Click"/>

                    <Button Grid.Column="1" Content="Обрати" FontSize="15" 
                        HorizontalAlignment="Right" Margin="0,0,-8,5" 
                        Foreground="Blue" Width="90" Style="{StaticResource FlatTextButton}"
                        Click="Select_Click"/>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>